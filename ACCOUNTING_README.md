# 记账业务管理系统使用说明

## 功能概述

记账业务管理系统是一个交互式的记账工具，支持通过自然语言输入记账信息，系统会自动结构化并存储到数据库。

## 主要功能

### 1. 添加记账记录
- 支持自然语言输入，例如：
  - "今天花了50元买了一杯咖啡"
  - "在超市购物花费200元，用微信支付"
  - "2024-01-15 打车花费30元"
- 系统会自动提取：
  - 金额（amount）
  - 类别（category）：餐饮、交通、购物、娱乐、医疗、教育等
  - 日期（date）
  - 描述（description）
  - 支付方式（payment_method）
  - 标签（tags）
  - 地点（location）

### 2. 查询记账记录
支持多种查询方式：
- 查询所有记录
- 按日期查询
- 按类别查询
- 按金额范围查询
- 按关键词搜索

### 3. 修改记账记录
- 通过记录ID找到记录
- 输入新的自然语言描述
- 系统重新结构化并更新

### 4. 删除记账记录
- 通过记录ID删除记录
- 需要确认操作

### 5. 切换表/目录
- 支持多个表/目录管理
- 可以在不同表之间切换

### 6. 查看所有表/目录
- 显示所有表/目录及其记录数

## 使用方法

### 启动系统

```bash
cd /Users/lululu/Documents/notes_M/找自己/好玩的项目/Personal-Agent-Assistant
conda activate personal-assistant
python accounting_manager.py
```

### 操作菜单

启动后会显示主菜单：

```
============================================================
记账管理系统
============================================================
当前表/目录: 记账

请选择操作：
  1. 添加记账记录（输入自然语言）
  2. 查询记账记录
  3. 修改记账记录
  4. 删除记账记录
  5. 切换表/目录
  6. 查看所有表/目录
  0. 退出
============================================================
```

### 使用示例

#### 添加记录
1. 选择 `1`
2. 输入自然语言，例如："今天花了50元买了一杯咖啡"
3. 系统会显示结构化结果
4. 确认保存（输入 `y`）

#### 查询记录
1. 选择 `2`
2. 选择查询方式（1-5）
3. 输入查询条件
4. 查看结果

#### 修改记录
1. 选择 `3`
2. 输入记录ID
3. 输入新的自然语言描述
4. 系统自动更新

#### 删除记录
1. 选择 `4`
2. 输入记录ID
3. 确认删除（输入 `y`）

## 技术特性

### LLM结构化
- 使用DeepSeek API进行文本结构化
- 优化的prompt模板，专门针对记账业务
- 自动提取关键信息

### 数据库存储
- 使用SQLite数据库
- 支持表/目录概念（虚拟目录）
- 完整的增删改查功能

### 交互式界面
- 清晰的菜单系统
- 友好的用户提示
- 实时反馈

## 测试

运行测试脚本验证功能：

```bash
python test_accounting.py
```

测试包括：
- LLM API连接测试
- 文本结构化测试
- 字段完整性验证

## 注意事项

1. 确保已配置好 `.env` 文件中的 `OPENAI_API_KEY` 和 `OPENAI_BASE_URL`
2. 数据库文件存储在 `./data/assistant.db`
3. 所有记账记录默认存储在"记账"表中
4. 可以通过切换表/目录功能管理不同的数据集合

